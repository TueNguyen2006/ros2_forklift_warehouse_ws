# Localization Lab

Purpose:

```text
Visualize wheel-odometry prediction plus visual-odometry correction in a small EKF.
```

The app imports localization logic from `warehouse_visual_localization.core.localization`.

